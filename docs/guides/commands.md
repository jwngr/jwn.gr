# Commands

Prefer `npm run <script>` so local behavior matches CI.

## Common Commands

- `npm install`
- `npm run start` - dev server
- `npm run build` - type check and production build
- `npm run preview` - serve the production build locally
- `npm run lint`
- `npm run format`
